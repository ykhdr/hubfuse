# pair <device> requires an existing hub connection (identity + TLS certs).
# With an empty HOME, it fails on missing identity before making any network call.
env HOME=$WORK/home
mkdir $WORK/home
! exec hubfuse pair somedevice
stderr 'error:.*identity|error:.*not joined|error:.*load'
! stderr 'Usage:'
