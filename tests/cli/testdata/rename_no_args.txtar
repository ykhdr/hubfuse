# Regression for issue #20: `hubfuse rename` with no arguments must show
# both the arg-validation error and the usage block.
! exec hubfuse rename
stderr 'accepts 1 arg\(s\), received 0'
stderr 'Usage:'
